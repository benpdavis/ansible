---
- name: deploy host docker compose
  copy:
    src: files/
    dest: /Apps/radarr/
  become: true

- name: docker compose up
  shell:
    cmd: "docker compose -f /Apps/radarr/docker-compose.yml up -d"
