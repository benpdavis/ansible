---
- name: deploy ollama docker compose
  copy:
    src: files/
    dest: /Apps/ollama/
  become: true

- name: docker compose up
  shell:
    cmd: "docker compose -f /Apps/ollama/docker-compose.yml up -d"
