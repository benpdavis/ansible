---
- name: deploy host docker compose
  copy:
    src: files/
    dest: /Apps/sonarr/
  become: true

- name: docker compose up
  shell:
    cmd: "docker compose -f /Apps/sonarr/docker-compose.yml up -d"
