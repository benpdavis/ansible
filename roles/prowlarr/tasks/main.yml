---
- name: deploy host docker compose
  copy:
    src: files/
    dest: /Apps/prowlarr/
  become: true

- name: docker compose up
  shell:
    cmd: "docker compose -f /Apps/prowlarr/docker-compose.yml up -d"
