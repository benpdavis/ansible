---
- name: deploy host docker compose
  copy:
    src: files/
    dest: /Apps/tdarr/
  become: true

- name: docker compose up
  shell:
    cmd: "docker compose -f /Apps/tdarr/docker-compose.yml up -d"
