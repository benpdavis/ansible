---
- name: deploy host docker compose
  copy:
    src: files/
    dest: /Apps/lidarr/
  become: true

- name: docker compose up
  shell:
    cmd: "docker compose -f /Apps/lidarr/docker-compose.yml up -d"
