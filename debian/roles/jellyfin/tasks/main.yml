---
- name: deploy host docker compose
  copy:
    src: files/
    dest: /Apps/jellyfin/
  become: true

- name: docker compose up
  shell:
    cmd: "docker compose -f /Apps/jellyfin/docker-compose.yml up -d"
