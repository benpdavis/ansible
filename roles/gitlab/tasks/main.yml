---
- name: deploy portainer docker compose
  copy:
    src: files/
    dest: /Apps/gitlab/
  become: true

- name: docker compose up
  shell:
    cmd: "docker compose -f /Apps/gitlab/docker-compose.yml up -d"
